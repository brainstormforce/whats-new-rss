{
  "name": "whats-new-rss",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc --sourceMap false && gulp sass:minify && gulp uglify",
    "start": "tsc --watch | gulp sass:watch"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-clean-css": "^4.3.0",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^5.1.0",
    "gulp-uglify": "^3.0.2",
    "node-sass": "^9.0.0",
    "typescript": "^5.3.3"
  }
}
